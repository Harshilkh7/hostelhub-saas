{
  "name": "hostelhub-saas-render-root",
  "private": true,
  "scripts": {
    "preview": "cd frontend && npm run preview -- --host 0.0.0.0 --port $PORT",
    "start": "cd frontend && npm run preview -- --host 0.0.0.0 --port $PORT"
  }
}
